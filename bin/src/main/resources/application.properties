spring.application.name=KimDungForum
server.port=8080

# ===================== SQL Server =====================
spring.datasource.url=jdbc:sqlserver://localhost:1433;databaseName=kimdung_db;encrypt=false
spring.datasource.username=khoi
spring.datasource.password=181120
spring.datasource.driver-class-name=com.microsoft.sqlserver.jdbc.SQLServerDriver

# ===================== JPA / Hibernate =====================
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.database-platform=org.hibernate.dialect.SQLServerDialect

# ===================== CORS (handled in code) =====================
# Vue dev server runs on http://localhost:5173
